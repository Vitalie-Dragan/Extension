"use strict";

// Write a JavaScript function that takes in a year as input and returns
// "Leap year" if the year is a leap year and "Not a leap year" otherwise.
// (A leap year is divisible by 4, but not divisible by 100 unless it is also
// divisible by 400.)

/**
 * Returns "Leap year" if the year is a leap year and "Not a leap year" otherwise.
 * @param {number} year A year.
 * @returns {"Leap year" | "Not a leap year"} "Leap year" or "Not a leap year".
 */
function isLeapYear(year) {
	// Your code here...
}

module.exports = exports = isLeapYear;
