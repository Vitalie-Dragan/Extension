"use strict";

// Write a JavaScript function that takes in a character as input and returns
// "Vowel" if the character is a vowel (a, e, i, o, u) and "Consonant" otherwise.

/**
 * Returns "Vowel" if the character is a vowel (a, e, i, o, u) and "Consonant"
 * otherwise.
 *
 * @param {string} character A character.
 * @returns {"Vowel" | "Consonant"} "Vowel" or "Consonant".
 */
function checkVowelOrConsonant(character) {
	// Your code here...
	if (character === "a" || "e" || "i" || "o" ) {
		
	}
}

module.exports = exports = checkVowelOrConsonant;
