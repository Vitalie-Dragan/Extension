"use strict";

// Write a JavaScript function that takes in two numbers as input and returns
// the larger number.

/**
 * Returns the larger of two numbers.
 * @param {number} num1 The first number.
 * @param {number} num2 The second number.
 * @returns {number} The larger of the two numbers.
 */
function findLargerNumber(num1, num2) {
	// Your code here...
}

module.exports = exports = findLargerNumber;
