// Write a function called makeAlbum() that accepts two parameters, `artistName` and `albumTitle`.
// The function should return a object containing these two pieces of information.
// example object format album = {artistName: "Aqua", albumTitle: "Aquarium"}

// Your code here...
function makeAlbum(artistName, albumTitle){
    let album = {
        artistName: artistName,
        albumTitle: albumTitle
    }
    return album;
}

module.exports = exports = makeAlbum;
